% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bisulfite_seq_model_Feb28_2026.R
\name{LVEX_fit_bisulfite_counts}
\alias{LVEX_fit_bisulfite_counts}
\title{Fit the LVE-X model for bisulfite sequencing methylation counts (single CpG)}
\usage{
LVEX_fit_bisulfite_counts(
  i,
  MC,
  UC,
  family = "BetaBinomial",
  z = NULL,
  w = NULL,
  fem_idx = NULL,
  theta_start = NULL,
  conf_level = 0.95,
  eps = 1e-12,
  bootstrap_on_fail = TRUE,
  bootstrap_B = 300,
  bootstrap_seed = 1,
  bootstrap_verbose = FALSE
)
}
\arguments{
\item{i}{Integer. Column index of the CpG/site to fit (i.e., which column of \code{MC}/\code{UC}).}

\item{MC}{Integer matrix (n x p). Methylated read counts; rows are samples, columns are sites.}

\item{UC}{Integer matrix (n x p). Unmethylated read counts; same dimension/order as \code{MC}.}

\item{family}{Character. Model family; currently only \code{"BetaBinomial"} is supported.}

\item{z}{Optional numeric matrix/data.frame (n x q). Covariates for \eqn{\nu} (Xa).
If \code{NULL}, an intercept-only model is used.}

\item{w}{Optional numeric matrix/data.frame (n x r). Covariates for \eqn{\pi} (Xi).
If \code{NULL}, an intercept-only model is used.}

\item{fem_idx}{Integer or logical vector. Indices (or mask) indicating female samples.
Must be provided.}

\item{theta_start}{Optional numeric vector. Starting values for optimization with length
\code{ncol(z) + ncol(w) + 1} after adding intercepts internally. If \code{NULL}, defaults to
zeros for regression coefficients and 10 for \code{kappa}.}

\item{conf_level}{Numeric in (0, 1). Confidence level for intervals (default 0.95).}

\item{eps}{Numeric. Small value used to clip probabilities to avoid 0/1 issues (default 1e-12).}

\item{bootstrap_on_fail}{Logical. If \code{TRUE}, uses parametric bootstrap to obtain CIs and
covariance if Hessian-based inference fails (default \code{TRUE}).}

\item{bootstrap_B}{Integer. Number of bootstrap replicates (default 300).}

\item{bootstrap_seed}{Integer. Random seed for bootstrap reproducibility (default 1).}

\item{bootstrap_verbose}{Logical. If \code{TRUE}, prints bootstrap progress (default \code{FALSE}).}
}
\value{
A list with the following components:
\describe{
\item{point_estimates}{Named vector of parameter estimates (regression coefficients and \code{kappa}).}
\item{standard_errors}{Named vector of standard errors if available; otherwise \code{NULL}.}
\item{covariance_matrix}{Variance-covariance matrix of parameter estimates if available; otherwise \code{NULL}.}
\item{pi_hat}{Numeric vector of fitted \eqn{\pi} for each sample.}
\item{nu_hat}{Numeric vector of fitted \eqn{\nu} for each sample.}
\item{pi_ci}{Matrix with columns \code{lower} and \code{upper} for \eqn{\pi}.}
\item{nu_ci}{Matrix with columns \code{lower} and \code{upper} for \eqn{\nu}.}
\item{conf_level}{The confidence level used.}
\item{ci_method}{Character string indicating CI method: \code{"delta-hessian"}, \code{"parametric-bootstrap"}, or \code{"none"}.}
\item{bootstrap_details}{Bootstrap output (if used), including bootstrap parameter draws for downstream prediction.}
}
}
\description{
Fits the LVE-X beta-binomial model at a single CpG/site using
methylated (MC) and unmethylated (UC) read count matrices from bisulfite sequencing.
The function estimates covariate-dependent parameters for the active-allele methylation
level \eqn{\nu} (Xa) and the inactive-allele methylation level \eqn{\pi} (Xi), and
provides confidence intervals using either a Hessian-based delta method or a
parametric bootstrap fallback.
}
\details{
Internally, the function:
\itemize{
\item Adds an intercept column to \code{z} and \code{w} (even if you already supplied matrices).
\item Optimizes the negative beta-binomial log-likelihood via \code{stats::optim()} with
method \code{"L-BFGS-B"} and constraint \code{kappa > 0}.
\item Attempts Hessian-based inference using \code{numDeriv::hessian()} (if installed).
\item Falls back to parametric bootstrap for CIs and covariance if Hessian inversion fails.
}
}
\examples{
# Example (toy) usage:
# n samples, p sites
# MC/UC are integer count matrices of the same dimension
# fem_idx indicates which rows are females
# z and w are covariate matrices (rows aligned to MC/UC)
#
# NOTE: Replace neg_bb_loglik / bootstrap_fit_and_ci with your package implementations.
\dontrun{
set.seed(1)
n <- 20; p <- 5
MC <- matrix(rpois(n*p, 5), n, p)
UC <- matrix(rpois(n*p, 5), n, p)
fem_idx <- sample(c(TRUE, FALSE), n, replace = TRUE)
z <- data.frame(age = rnorm(n))
w <- data.frame(batch = rnorm(n))

out <- LVEX_fit_bisulfite_counts(
  i = 2, MC = MC, UC = UC,
  z = z, w = w, fem_idx = fem_idx
)
str(out)
}

}
