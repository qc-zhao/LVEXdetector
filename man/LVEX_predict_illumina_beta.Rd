% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/illumina_model_Mar17.R
\name{LVEX_predict_illumina_beta}
\alias{LVEX_predict_illumina_beta}
\title{Predict nu (Xa) and pi (Xi) for new samples from a fitted Illumina LVE-X Beta model}
\usage{
LVEX_predict_illumina_beta(fit, new_z, new_w, conf_level = NULL)
}
\arguments{
\item{fit}{A fitted model object returned by \code{LVEX_fit_illumina_beta()}.
It must contain \code{point_estimates}, and ideally \code{covariance_matrix} and/or
\code{bootstrap_details$par_boot}.}

\item{new_z}{Numeric matrix/data.frame. Covariates for \eqn{\nu} (Xa) on new
samples. Column names must match those used in model fitting, excluding the
intercept, which is added automatically.}

\item{new_w}{Numeric matrix/data.frame. Covariates for \eqn{\pi} (Xi) on new
samples. Column names must match those used in model fitting, excluding the
intercept, which is added automatically.}

\item{conf_level}{Optional numeric in (0,1). Confidence level for prediction
intervals. If \code{NULL}, uses \code{fit$conf_level} if available, otherwise 0.95.}
}
\value{
A data frame with one row per new sample and columns:
\describe{
\item{nu_hat}{Point prediction for \eqn{\nu}.}
\item{nu_low}{Lower confidence bound for \eqn{\nu}.}
\item{nu_high}{Upper confidence bound for \eqn{\nu}.}
\item{pi_hat}{Point prediction for \eqn{\pi}.}
\item{pi_low}{Lower confidence bound for \eqn{\pi}.}
\item{pi_high}{Upper confidence bound for \eqn{\pi}.}
\item{ci_method}{Method used for prediction intervals: \code{"bootstrap-predict"}, \code{"delta-hessian"}, or \code{"none"}.}
}
}
\description{
Generates point predictions and confidence intervals for the active-allele
methylation level \eqn{\nu} (Xa) and inactive-allele methylation level
\eqn{\pi} (Xi) for new covariate values, using a fitted object returned by
\code{LVEX_fit_illumina_beta()}.
}
\details{
The function prefers bootstrap-based prediction intervals if bootstrap
parameter draws are available; otherwise it falls back to delta-method
intervals using the fitted covariance matrix.

The coefficient-to-covariate mapping is inferred from \code{fit$point_estimates}:
parameters starting with \code{a_} are used for the \eqn{\nu} model, and those
starting with \code{b_} are used for the \eqn{\pi} model.

Bootstrap prediction intervals are formed by transforming bootstrap parameter
draws through the logistic link and taking empirical quantiles. If bootstrap
draws are not available, the function uses delta-method intervals based on the
covariance matrix.
}
\examples{
\dontrun{
new_z <- data.frame(age = c(50, 60))
new_w <- data.frame(batch = c(0.1, -0.2))
pred <- LVEX_predict_illumina_beta(fit, new_z = new_z, new_w = new_w)
pred
}

}
