% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/illumina_model_Mar17.R
\name{LVEX_fit_illumina_beta}
\alias{LVEX_fit_illumina_beta}
\title{Fit the LVE-X model for Illumina methylation beta values at a single CpG}
\usage{
LVEX_fit_illumina_beta(
  i,
  BetaMat,
  family = "Beta",
  z = NULL,
  w = NULL,
  fem_idx = NULL,
  theta_start = NULL,
  conf_level = 0.95,
  eps = 1e-12,
  bootstrap_on_fail = TRUE,
  bootstrap_B = 300,
  bootstrap_seed = 1,
  bootstrap_verbose = FALSE
)
}
\arguments{
\item{i}{Integer. Column index of the CpG/site to fit, i.e. which column of \code{BetaMat}.}

\item{BetaMat}{Numeric matrix (n x p). Illumina methylation beta values;
rows are samples and columns are CpG sites. Values should lie in 0 and 1.}

\item{family}{Character. Model family; currently only \code{"Beta"} is supported.}

\item{z}{Optional numeric matrix/data.frame (n x q). Covariates for
\eqn{\nu} (Xa). If \code{NULL}, an intercept-only model is used.}

\item{w}{Optional numeric matrix/data.frame (n x r). Covariates for
\eqn{\pi} (Xi). If \code{NULL}, an intercept-only model is used.}

\item{fem_idx}{Integer or logical vector indicating female samples.
Must be supplied.}

\item{theta_start}{Optional numeric vector of starting values for optimization.
Its required length is \code{ncol(z) + ncol(w) + 1} after intercepts are added
internally. If \code{NULL}, defaults to zeros for regression coefficients and 10
for \code{kappa}.}

\item{conf_level}{Numeric in (0,1). Confidence level for intervals.
Default is 0.95.}

\item{eps}{Numeric. Small value used to clip beta values and probabilities
away from 0 and 1 to avoid numerical issues. Default is \code{1e-12}.}

\item{bootstrap_on_fail}{Logical. If \code{TRUE}, performs parametric bootstrap
when Hessian-based inference fails. Default is \code{TRUE}.}

\item{bootstrap_B}{Integer. Number of bootstrap replicates. Default is 300.}

\item{bootstrap_seed}{Integer. Random seed for bootstrap reproducibility.
Default is 1.}

\item{bootstrap_verbose}{Logical. If \code{TRUE}, prints bootstrap messages.
Default is \code{FALSE}.}
}
\value{
A list with components:
\describe{
\item{point_estimates}{Named vector of parameter estimates.}
\item{standard_errors}{Named vector of standard errors, if available.}
\item{covariance_matrix}{Variance-covariance matrix of the parameter estimates, if available.}
\item{pi_hat}{Fitted sample-level \eqn{\pi} values.}
\item{nu_hat}{Fitted sample-level \eqn{\nu} values.}
\item{pi_ci}{Confidence interval matrix for \eqn{\pi}, with columns \code{lower} and \code{upper}.}
\item{nu_ci}{Confidence interval matrix for \eqn{\nu}, with columns \code{lower} and \code{upper}.}
\item{conf_level}{Confidence level used.}
\item{ci_method}{Method used for intervals: \code{"delta-hessian"}, \code{"parametric-bootstrap"}, or \code{"none"}.}
\item{bootstrap_details}{Bootstrap output if bootstrap was used, including parameter draws.}
}
}
\description{
Fits the LVE-X Beta model at one CpG/site using Illumina-array methylation
beta values (continuous outcomes in (0,1)). The function estimates
covariate-dependent parameters for the active-allele methylation level
\eqn{\nu} (Xa) and the inactive-allele methylation level \eqn{\pi} (Xi),
while allowing female samples to follow the LVE-X mixture mean structure and
male samples to follow a simpler Beta model with mean \eqn{\nu}.
}
\details{
Confidence intervals are obtained using either:
\itemize{
\item a Hessian-based delta method, or
\item a parametric bootstrap fallback if Hessian-based inference fails.
}

Internally, the function:
\itemize{
\item adds intercept columns to \code{z} and \code{w},
\item optimizes the negative log-likelihood using \code{stats::optim()} with
method \code{"L-BFGS-B"} and constraint \code{kappa > 0},
\item attempts Hessian-based inference using \code{numDeriv::hessian()},
\item falls back to parametric bootstrap if Hessian-based inference is unavailable or unstable.
}
}
\examples{
\dontrun{
set.seed(1)
n <- 20; p <- 5
BetaMat <- matrix(runif(n * p, 0.05, 0.95), n, p)
fem_idx <- sample(c(TRUE, FALSE), n, replace = TRUE)
z <- data.frame(age = rnorm(n))
w <- data.frame(batch = rnorm(n))

fit <- LVEX_fit_illumina_beta(
  i = 2,
  BetaMat = BetaMat,
  z = z,
  w = w,
  fem_idx = fem_idx
)
str(fit)
}

}
