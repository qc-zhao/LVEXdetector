% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bisulfite_seq_model_Feb28_2026.R
\name{LVEX_predict_bisulfite_counts}
\alias{LVEX_predict_bisulfite_counts}
\title{Predict nu (Xa) and pi (Xi) for new samples from a fitted bisulfite-counts LVE-X model}
\usage{
LVEX_predict_bisulfite_counts(fit, new_z, new_w, conf_level = NULL)
}
\arguments{
\item{fit}{A fitted model object returned by \code{LVEX_fit_bisulfite_counts()}.
It must contain \code{point_estimates}, and ideally \code{covariance_matrix} and/or
\code{bootstrap_details$par_boot}.}

\item{new_z}{Numeric matrix/data.frame. Covariates for \eqn{\nu} (Xa) on new samples,
with column names matching the covariates used in the fitting function (excluding
the intercept, which is added automatically).}

\item{new_w}{Numeric matrix/data.frame. Covariates for \eqn{\pi} (Xi) on new samples,
with column names matching the covariates used in the fitting function (excluding
the intercept, which is added automatically).}

\item{conf_level}{Optional numeric in (0, 1). Confidence level for intervals.
If \code{NULL}, the function uses \code{fit$conf_level} if available, otherwise defaults to 0.95.}
}
\value{
A data frame with one row per new sample and columns:
\describe{
\item{nu_hat}{Point prediction for \eqn{\nu}.}
\item{nu_low}{Lower confidence bound for \eqn{\nu}.}
\item{nu_high}{Upper confidence bound for \eqn{\nu}.}
\item{pi_hat}{Point prediction for \eqn{\pi}.}
\item{pi_low}{Lower confidence bound for \eqn{\pi}.}
\item{pi_high}{Upper confidence bound for \eqn{\pi}.}
\item{ci_method}{Which method was used: \code{"bootstrap-predict"}, \code{"delta-hessian"}, or \code{"none"}.}
}
}
\description{
Generates point predictions and confidence intervals for the active-allele methylation
level \eqn{\nu} (Xa) and the inactive-allele methylation level \eqn{\pi} (Xi) for new
covariate values, using a model fitted by \code{LVEX_fit_bisulfite_counts()}.
}
\details{
The function prefers bootstrap-based prediction intervals if the fitted object contains
bootstrap parameter draws; otherwise it falls back to delta-method intervals using the
fitted covariance matrix.

The mapping between coefficients and covariates is inferred from the names in
\code{fit$point_estimates}:
\itemize{
\item Parameters starting with \code{a_} are used for the \eqn{\nu} model.
\item Parameters starting with \code{b_} are used for the \eqn{\pi} model.
}
Therefore, the column names of \code{new_z} and \code{new_w} must contain the same covariate
names used in fitting (excluding the intercept).

Bootstrap prediction intervals are computed by transforming each bootstrap draw of
parameters through the logistic link and taking empirical quantiles.
Delta-method intervals use \code{delta_ci_logit()} with the appropriate sub-block of the
covariance matrix.
}
\examples{
\dontrun{
# Suppose `fit` is returned by LVEX_fit_bisulfite_counts()
new_z <- data.frame(age = c(50, 60))
new_w <- data.frame(batch = c(0.1, -0.2))
pred <- LVEX_predict_bisulfite_counts(fit, new_z = new_z, new_w = new_w)
pred
}

}
